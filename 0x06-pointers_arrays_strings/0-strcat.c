#include "main.h"
/**
 * _strcat - concatenate strings
 * @dest: character string
 * @src: character string
 * return: character of dest
 */
char *_strcat(char *dest, char *src)
{
	cat = _strcat(dest, src);
	return (cat);
}
